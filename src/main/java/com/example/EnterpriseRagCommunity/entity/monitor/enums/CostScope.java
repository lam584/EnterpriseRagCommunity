package com.example.EnterpriseRagCommunity.entity.monitor.enums;

/**
 * 枚举：费用范围/用途，与表 cost_records.scope ENUM('GEN','RERANK','MODERATION','OTHER') 精确对齐
 */
public enum CostScope {
    GEN,
    RERANK,
    MODERATION,
    OTHER
}

