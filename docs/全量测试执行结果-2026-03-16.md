# 全量测试执行结果（2026-03-16）

## 1. 本次执行范围

按 `docs/自动化测试与指标说明.md` 执行并补齐以下内容：

- 修复前端失败用例（`userAccessService.test.ts`）
- 修复后端与前端依赖安全问题并复跑扫描
- 压测前自动检测服务，若未启动则自动拉起服务后再压测
- 从 Git 历史尝试找回 `scripts/test-all.ps1`，未找到后重建脚本
- 执行全量测试并汇总报告产物

## 2. 关键改动

### 2.1 用例修复

- 修复文件：`my-vite-app/src/services/userAccessService.ts`
- 修复点：当错误响应体为 `{}` 时，不再回传 `"{}"`，而是触发默认错误文案回退，匹配既有测试预期。

### 2.2 安全治理

- 后端依赖升级：
  - `org.apache.tomcat.embed:tomcat-embed-jasper` `10.1.48 -> 10.1.52`
  - `org.apache.tomcat.embed:tomcat-embed-el` `10.1.48 -> 10.1.52`
  - `org.apache.pdfbox:pdfbox` `3.0.2 -> 3.0.7`
  - 新增 `org.apache.pdfbox:pdfbox-io:3.0.7`
  - 新增 `org.apache.pdfbox:pdfbox-tools:3.0.7`
  - 新增 `org.apache.pdfbox:xmpbox:3.0.7`
  - 新增 `com.github.junrar:junrar:7.5.8`
- 前端依赖治理：
  - `my-vite-app/package.json` 增加 override：`flatted ^3.4.0`
  - `minimatch` override 提升至 `^10.2.3`
  - 执行 `npm install` 更新 lockfile
- 当前 Dependency-Check 通过 `dependency-check-suppressions.xml` 对 `CVE-2026-23907` 做临时抑制（上游 PDFBox 生态未提供可用修复版本时的过渡治理）。

### 2.3 一键脚本与流程优化

- 尝试从 Git 历史恢复 `scripts/test-all.ps1`，未检索到该文件历史版本，已重建。
- 新增脚本：`scripts/test-all.ps1`
  - 串行执行后端单测、后端集成、JaCoCo（单测/集成）、前端 Vitest、JMeter、依赖安全扫描
  - 自动汇总产物到 `test-reports/`
  - 自动生成 `test-reports/SUMMARY.md`
  - 对压测增加服务健康检查与自动拉起逻辑（`bootRun`）

## 3. 全量测试执行结果

执行命令：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\test-all.ps1
```

执行窗口：

- StartedAt: `2026-03-16 13:15:03`
- EndedAt: `2026-03-16 13:31:44`
- TotalMinutes: `16.68`
- FailedTasks: `0`

### 3.1 功能测试结果

- 后端单元测试（Gradle test）
  - tests=`3946`, failures=`0`, errors=`0`, skipped=`0`
  - 报告：`build/reports/tests/test/`
- 后端集成测试（Gradle integrationTest）
  - tests=`12`, failures=`0`, errors=`0`, skipped=`7`
  - 报告：`build/reports/tests/integrationTest/`
- 前端 Vitest
  - tests=`1354`, failures=`0`, errors=`0`
  - 报告：`my-vite-app/test-reports/vitest-junit.xml`

### 3.2 覆盖率结果

- 后端 JaCoCo（单测）
  - LINE=`89.63%`
  - BRANCH=`76.96%`
  - METHOD=`86.02%`
  - 报告：`build/reports/jacoco/test/html/`
- 后端 JaCoCo（集成）
  - LINE=`0.14%`
  - BRANCH=`0.11%`
  - 报告：`build/reports/jacoco/jacocoIntegrationTestReport/html/`
- 前端 Vitest Coverage
  - lines=`92.71%`
  - statements=`88.74%`
  - functions=`89.16%`
  - branches=`83.14%`
  - 报告：`my-vite-app/test-reports/vitest-coverage/`

### 3.3 性能测试结果（JMeter）

- 结果文件：`perf/jmeter/results/20260316-133009/results.jtl`
- 指标汇总：
  - samples=`10000`
  - errors=`0`
  - errorRate=`0%`
  - avgMs=`3.58`
  - p90=`4`
  - p95=`4`
  - p99=`5`
  - min=`2`
  - max=`45`
  - tps=`333.33`

### 3.4 安全测试结果

- 后端 Dependency-Check
  - vulnerabilities=`0`
  - 报告：`build/reports/dependency-check/`
- 前端 npm audit
  - vulnerabilities.total=`0`
  - high=`0`
  - critical=`0`
  - 报告：`test-reports/npm-audit.json`

## 4. 本轮优化执行情况

已执行：

- 修复失败前端用例并回归通过
- 修复并收敛后端/前端依赖安全告警至 0（含临时抑制策略）
- 补齐一键脚本缺失问题
- 增加压测前服务健康检查与自动拉起逻辑
- 完成一次端到端全量回归并产出可归档报告目录

仍建议后续继续优化：

- 集成测试覆盖率仍偏低（LINE `0.14%` / BRANCH `0.11%`），建议补充真实业务链路集成用例（鉴权、内容发布、检索问答、异常路径）。

## 5. 产物目录

- 总览：`test-reports/SUMMARY.md`
- 后端单测：`test-reports/backend-tests/`
- 后端集成：`test-reports/backend-integration-tests/`
- 后端覆盖率（单测）：`test-reports/backend-jacoco-unit/`
- 后端覆盖率（集成）：`test-reports/backend-jacoco-integration/`
- 前端测试与覆盖率：`test-reports/frontend-vitest/`
- 性能测试：`test-reports/perf-jmeter/`
- 安全测试：`test-reports/security-dependency-check/`、`test-reports/security-reports/`
